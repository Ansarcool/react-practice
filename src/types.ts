export type Product = {
    name: string;
    price: number;
    categoryId: number;
    id: string;
}
export type ProductFormState = {
    name: string;
    price: string;
    category: string;
}
export type Categories = {
    id: number,
    name: string
}
